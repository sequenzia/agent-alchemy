[project]
name = "agent-alchemy"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]

dev = []

docs = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.26",
    "mike>=2.0",
]

[tool.uv]

default-groups = ["dev", "docs"]
